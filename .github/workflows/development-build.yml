name: Development build

on:
  push:
    branches:
      - "**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-linux-x86_64:
    name: Linux x86_64
    permissions:
      contents: read
    uses: ./.github/workflows/linux-build.yml
    with:
      checkout-ref: ${{ github.sha }}
      build-label: development
      archive-name: aishell-development-linux-x86_64.tar.gz
      artifact-name: aishell-development-linux-x86_64-${{ github.sha }}
      retention-days: 14

  build-windows-x86_64:
    name: Windows x86_64
    permissions:
      contents: read
    uses: ./.github/workflows/windows-build.yml
    with:
      checkout-ref: ${{ github.sha }}
      build-label: development
      archive-name: aishell-development-windows-x86_64.zip
      artifact-name: aishell-development-windows-x86_64-${{ github.sha }}
      retention-days: 14
